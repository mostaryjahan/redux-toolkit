import { useSelector } from "react-redux";

const PostView = () => {
    const user = useSelector
    return (
        <div>
            <h3>Post View</h3>

        </div>
    );
};

export default PostView;